import 'package:flutter/material.dart';

class AirtelContacts extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Airtel Contacts'),
      ),
      body: Center(
        // Display Airtel contacts or relevant content
        child: Text('Airtel Contacts Page Content'),
      ),
    );
  }
}
